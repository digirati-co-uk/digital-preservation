from app.result import Result


def add_descriptive_metadata_to_manifest(manifest, descriptive_metadata):
    pass

def add_painted_resources(manifest, archival_group, mets) -> Result:
    message = f"Could not turn METS file information into painted resources: (error message)"
    return {}